import { Router } from "express";

const routes = Router()

routes.get('/register', )
routes.get('/login', )

routes.post('/regiQster', )
routes.post('/login', )
routes.post('/forget', )


export default routes